// Define if we want to use MPI
// NOTE: If the CMake variable HAVE_${POJECT_NAME}_MPI is defined the macro
//       HAVE_${POJECT_NAME}_MPI will be set to 1. If the CMake variable 
//       does not exists, the macro will be set to 0. Therefore MPI
//       functionality is turned off by default.
//#cmakedefine HAVE_${PROJECT_NAME}_MPI
#define HAVE_${PROJECT_NAME}_MPI ${HAVE_${PROJECT_NAME}_MPI}
