INCLUDE_DIRECTORIES(${${PROJECT_NAME}_INCLUDE_DIRS})

ADD_EXECUTABLE(main main.cpp)

TARGET_LINK_LIBRARIES(main dl ${${PROJECT_NAME}_LIBS} ${PROJECT_NAME})

INSTALL(
  TARGETS main
  RUNTIME DESTINATION bin
  COMPONENT main
  )
